import { Card, Typography } from "@mui/material";

export default function LoginPage() {
  return (
    <Card sx={{ marginX: "auto", maxWidth: "400px" }}>
      <Typography variant="h6">Login</Typography>
    </Card>
  );
}
